# Polish lane V — integrated browser verification

## Final verification record

**Accepted final product:** `d1766fdf741a18336b975eb891a89f28d9ecd4ab`. Independent composite verification passed **500 effective cases /6,764 checks**, comprising 410 retained cases from the full matrix at `2f23b8f5` and **90 affected cold-route replacements** at the final product. Coverage is **450 cold loads /760 settled month observations**, both themes at all five requested viewports. Maximum effective CLS is **0.068610**, below 0.1; maximum month-height spread is **1px**. The replacement run used probe `2ea22838f37c152f53a840f354a06fe49d2fc429` and passed all **1,368 checks**, with zero driver or serialized browser/API/dialog/write errors. This is composite acceptance with explicit carried-forward evidence, rather than a new whole 500-case run at the final commit.

The full matrix completed all **500 cases /450 cold loads /760 settled month observations** across both themes and all five requested viewports. It recorded **6,763 passing checks and one failing check**, a real Net worth cold-load CLS of **0.166327** at 1280 light. A separate audit of the serialized logs also found **nine late teardown errors in three ESPP cases** whose earlier mutable-log checks had reported green. The raw report remains unchanged, with SHA-256 `38cb0059686e44ccbdd32a8e5a3b47f6ed1292efa89b45595de27721a17f5eae`, at `scratchpad/polish-v/final-2f23b8f5/report.json`.

The final supplement covers Net worth, all five Portfolio variants, and all three ESPP variants at every viewport/theme combination. It addresses the ScopeBar owner-control arrival fix, the measured ESPP long-title overflow correction, and the three instrument-failed ESPP cases. The harness now tracks complete asynchronous route handlers, waits for a fresh quiet interval, closes the context, drains cancellation callbacks, and returns a detached log snapshot. Four lifecycle checks pass, including late requests, fulfillment, rejection and bounded timeout; timeout remains a failed case. Root independently reviewed this probe change. No network error is suppressed.

The independent `scratchpad/codex-cosmetic-final/audit-composite.mjs` validator on root main passed. It pinned the raw failure roster, exactly 90 authorized replacements, source/product/probe hashes, all original per-case assertions, **500 unique effective case IDs /450 CLS values /760 month observations**, clean effective error logs, and all **40 recorded badge top offsets at 0px**. Its result is `scratchpad/codex-cosmetic-final/audit-composite-final-d1766fdf-result.json`, SHA-256 `f29b947f5621f0e2025c410aad370afe6058d83c8499f163ad5861a2723ad41d`. The raw run's **12 retry records** remain retained: nine relate to the three superseded teardown-error cases; three were successful socket retries in two unchanged layout cases. The supplement adds seven successful socket/reset retries, making **ten recovered retries in effective coverage**, with no final request failures. Recovered transport retries are observations, not unreported failures or a claim of retry-free transport.

The replacement report is `scratchpad/polish-v/affected-final-d1766fdf/report.json`, SHA-256 `8788480a45aa25ac920c35d0837b9c0601f9b7e74ea2faaf551b6837752d7a56`, completed **11:28:28.909–11:34:53.495Z on 2026-09-26**. Its console log is `scratchpad/polish-v/affected-final-d1766fdf-run.log`. The original report, its log, prior failed attempts, source archive, fresh mutation reports, correction proofs and visual-review notes are preserved. The current indexes are `scratchpad/polish-v/fresh-evidence.json` and root `scratchpad/codex-cosmetic-final/{manifest,final-fixes}.json`.

### Measured results and correction evidence

| Surface | Earlier audit or retained failure | Integrated observation |
|---|---|---|
| Sidebar | Comfortable overflow 86/118/22px at short sizes; compact 18/50px | All measured sidebar overflow 0px. Full email and red logout remain visible on one centerline; comfortable large-screen spacing is retained. |
| Five chart pairs | Bottom mismatch 63/69/95/17/43px | Maximum mismatch 0px; real Table open/close and full reveal were checked. Focused Table reveal is 363.78px versus the earlier 21px visible strip. |
| Overview Changes / Settings actions | Changes blank 109px; Settings gaps 261/173px | Changes blank 0px, maximum pinned-action gap 92.5px, required natural-height spread 14.744%. |
| Calendar / Allocation / meters | Weekday band 65px; allocation height spread 294px; budget/paycheck end offsets 7/27px | Weekday text band at most 8px; allocation and both meter-end spreads 0px. |
| Tiles / months | Badge value offset 17–19px; Net worth row heights 145/111/128px; mixed bare-tile blank bands 35–58px | Value baseline and tile-height spreads 0px. All 760 correctly identified, settled month observations stay within a 1px row-height spread; every 1440px month delta fits one line. Required second lines are present, with only the documented Realized gains / household-wallet exceptions allowed. |
| What-if loading | Cold CLS 0.122786 /0.248445 | Twenty affected cold cases passed, maximum CLS 0.023983; exact Apply/Undo and dirty-draft guards were replayed afterward. |
| ScopeBar late household | Raw Net worth CLS 0.166327; forced-delay movement 42.1875px and CLS 0.166560 | Four forced before/after cases: movement 0px after the fix, maximum after CLS 0.005675. The measured working diff exactly matches committed `40ac36a3`. All ten final Net worth cold cases passed; maximum CLS 0.000566. |
| ESPP long tile title | Four of ten geometry cases overflowed; 25.19px at 1366 and 10.41px at 1440 | Ten final geometry cases have no overflow after page-local wrapping. All thirty ESPP cold replacements passed; maximum CLS 0.068610. |

The user's final cosmetics are covered separately from the original audit: the footer is 37px high rather than the superseded two-row proposal's 54px; badges share the title header without a dedicated badge track; a nonbreaking inline wrapper keeps each info affordance with the title's last word. The 60-observation header diagnostic passed at all five sizes and both themes, with maximum row spread 1px and header spread 0.96875px. Its measured source diff exactly matches `2f23b8f5`. Real pending-evidence tooltips passed in both themes and kept the header at 32px before/after evidence arrival. Failed generated-joiner and plain-NBSP candidates remain retained preparation artifacts.

### Evidence, tests and limits

Fresh L5–L8 mutation evidence is indexed with its actual product head in ignored `scratchpad/polish-v/fresh-evidence.json`; the later section records exact artifact paths and restoration proof. L5 has 55 checks, L6 has 16, L7 has 20, and L8 has four theme groups. Existing records and dependent identities were restored exactly, temporary fixtures removed, and intentional L5 409/500 fault responses remain explicitly separate from unexpected errors. Reduced-motion coverage is 1280 light for L7 and 1366 dark for the native-date focus regression. These flows were not relabelled as new final-head runs when unchanged surfaces were carried forward.

The last fully green integrated frontend suite is **c30c76b: 324 suites /4,872 tests**, with app/node noEmit, ESLint 0 errors /26 existing warnings, and build passing. At `0b3290c2`, the next full run had **4,875 passing tests and one asynchronous TransactionsPanel test failure**; the test-only wait correction at `01b1aed3` passed all 73 affected tests. The user then explicitly requested no further heavyweight run for the cosmetics. Final focused evidence includes 50 tile component tests, 43 sidebar tests, 41 ScopeBar tests, app noEmit and targeted lint. The final inline wrapper's 50 component tests/lint and the earlier 20+3 page compatibility tests have their precise source attribution in root `scratchpad/codex-cosmetic-final/manifest.json`. Backend remains **2,845 passed /4 skipped**, with unchanged tree `9dcde78043e5d9dbb13e1ed53de82c1e601919c6`.

Root's `scratchpad/codex-cosmetic-final/final-fixes.json` hashes the forced ScopeBar proof, both ESPP geometry reports and the ordinary preview report. The ordinary preview passed **4/4** final-head checks (Net worth and ESPP, dark/light, 1366px) through the normal Vite proxy, with real tiles, no horizontal overflow, complete footer, and no browser/network/dialog/fenced-write errors. V's matrix instead uses the documented transparent forwarding to the private backend; this transport distinction remains explicit.

Visual review is complete for representative successful screenshots in all ten viewport/theme combinations, every threshold failure, and final replacement Net worth/Portfolio/ESPP screenshots. The **37 attributed observations** in ignored `scratchpad/polish-v/visual-review.json` distinguish actual committed source, measured candidates later committed unchanged, intended overlays, and geometry from theme-palette evidence. There are no remaining acceptance blockers in this scope. The fresh source was one read-only production dump acquired at **07:33:31Z on 2026-09-26**, SHA-256 `bb765dd3c70f20cc16f9d741936dcc6db1d0b36f83d55b58805bc1b084e210a8`; both private copies initially matched all 45 tables /27,244 rows. No production writes occurred.

Cleanup completed **11:36:55Z**: V closed its browsers and verified/stopped **5279/PID25556, 5280/PID34532 and 8089/PID25416**, leaving no owned listeners. Root's user preview **5281/PID15392 and 8091/PID37396** remains running; frontend HTTP200 and authenticated API200 were rechecked. Private databases, runtime paths, worktree and all artifacts were retained. Cleanup evidence is `scratchpad/polish-v/cleanup.json`. No push or deployment occurred.

## Original plan and historical execution record

**Authority:** approved design `../specs/2026-09-25-polish-alignment-feedback-undo-design.md`, especially §9, and `2026-09-25-polish-00-overview.md`. This lane prepares and runs the final browser acceptance on merged local main. No push or deploy.

**Ownership:** this plan and `tools/probes/polish-v/` only. Product defects go to the coordinator with measurements, reproduction and the responsible surface. Screenshots, dumps, tokens, generated configs, logs and JSON reports stay in ignored scratch storage. No changes to another lane's worktree or shared `node_modules`.

**Later user direction (2026-09-26):** show the full footer email, make logout red, and place each tile badge at the top right beside its title without a dedicated blank badge row. The user's follow-up keeps the identity beside the actions in one centered footer row; the first proposed identity-above-actions layout was superseded before final verification. Continue verification without another heavyweight test run for these cosmetic changes; preserve the actual earlier gate results and run the browser acceptance against the final merged appearance.

## 1. Source and environment

- [x] Read the approved spec, overview and L1/L2 as-built records; recover the original audit harness and L1/L2/L5 measurement scripts.
- [x] Inspect for an existing verification worktree. None existed. Create `.worktrees/polish-verify`, branch `feat/polish-verify`, from main `204ef73d`; attach a junction to root `node_modules`.
- [x] Before final acceptance, update this branch to the coordinator's merged L5–L8 main. Record the exact product commit in every report. A preparation run cannot produce a final acceptance verdict.
- [x] Take one read-only production `pg_dump` through the established SSH/container procedure; record UTC acquisition time, source schema, archive SHA-256 and size. Never call a production mutation, trigger a production backup job, or alter production credentials.
- [x] Restore that same dump into **new**, private `finance_polish_v_read` and `finance_polish_v_write` databases on Docker Postgres `127.0.0.1:5433`. Refuse an existing destination; do not drop or overwrite a database. Record matching table counts before local auth setup.
- [x] Run the read copy on `127.0.0.1:8089`, writable twin on `127.0.0.1:8091`; scheduler and snapshots disabled, `PRODUCT_TODAY=2026-09-25` for comparison with the audit. Port 8090 belongs to `WsToastNotification.exe` and is not used.
- [x] Run worktree Vites on 5279/5280 with separate private caches, explicit API targets and strict ports. The browser fence forwards real API reads directly to the matching loopback backend (established table-scroll/L6 workaround for intermittent Vite proxy truncation); report this transport choice.
- [x] Keep authentication secrets in ignored token files, never logs or tracked manifests. Local auth setup is allowed only on the two V copies. The default probe fences mutations; writable mode additionally requires an explicit twin URL and local source manifest.
- [x] Verify `/auth/me`, schema and local database identity before opening a browser. Both identities/schema were reconfirmed after the final product update; the read copy reports read-only transactions. Close each context/browser in `finally`; stop only V-owned servers on completion.

The user later requested an ordinary local preview. Ownership of writable backend **8091** and its existing private data/runtime directories transferred to the coordinator for that preview; V must leave it running. V's eventual cleanup is limited to **8089/5279/5280** and its own headless browsers. The coordinator separately verifies ordinary preview proxy traffic because the V harness uses the explicit direct-backend transport described above.

The older audit copy (`finance_polish_audit`) and lane copies (`finance_polish_w5`–`w8`) supply historical evidence, never the final fresh-copy verdict. The original read-only dump invocation was recovered by the Settings agent; V executed it once and restored identical bytes into both private databases.

## 2. Reusable driver and report

- [x] Implement `tools/probes/polish-v/smoke.mjs`, a loopback-only Edge driver with classic scrollbars, one browser/context at a time, a mutation fence, native-dialog/console/page/network error collection, screenshots, per-check evidence and nonzero exit on failed or absent coverage.
- [x] Recover tile baseline instrumentation from L2 (temporary zero-size inline marker removed within the same evaluation), chart selectors/measurements from L1, and Settings natural-height/action-gap measurements from L5.
- [x] Put the audit's before-numbers and source references in a tracked baseline manifest. Keep observations separate from passes: missing elements, absent rows, driver errors and unexercised branches cannot silently pass.
- [x] Add filters for groups, routes, dimensions and theme that reject unknown values. Reports name applied filters. Acceptance requires the full matrix plus required flow evidence; later authorized affected replacements are checked by a separate composite validator and never rewrite the raw full report.
- [x] Require an explicit merged product SHA and fresh-source manifest for final runs. `PREPARE=1` allows a labelled smoke while authoring; it never claims acceptance.
- [x] Write progressive JSON after each case so interrupted work is reviewable. Redact secrets and do not store API payloads containing the private book in tracked files.

## 3. Matrix: both themes at every viewport

Each group runs dark and light at **1280×800, 1366×768, 1440×900, 1536×864, 1920×1080**. Sidebar additionally checks comfortable and compact density. The fresh flow artifacts separately verify reduced motion at **1280×800 light** (L7 budget/matrix) and **1366×768 dark** (L6 native-date focus, alongside normal motion). The original plan named 1440×900 dark; the actual fresh trace used the smaller viewport where the focus defect was reproduced. The 500-case matrix uses normal motion.

| Group | Acceptance and actual measurement |
|---|---|
| Sidebar (§2, updated user direction) | `scrollHeight - clientHeight <= 1`; full email visible beside 28×28 theme/logout buttons on the same centerline; logout red; Search… not truncated; all navigation links present. Confirm normal rhythm is retained above 900px height. |
| Five chart pairs (§3.1) | Overview Portfolio performance/Recent spending, Spending Trends, Paycheck breakdown/flow, ESPP anatomy/price, card detail credits/limit history: bottom difference <=1px with Tables closed, each available Table open, and closed again. Trends plot tops and bottoms also <=1px. Record before/after. |
| Overview (§3.2) | Wealth/agenda bottoms <=1px; Changes blank band <=24px with trend Table closed/open/closed. Customize hide/show leaves no isolated half-width hole or horizontal overflow. |
| Settings (§3.3) | Walk all five tabs; each real paired row bottoms <=1px; gap above pinned actions <=96px. At 1440/1920 record natural-height spread <=15% for pairs covered by the pairing rule, with Appearance/Password's explicitly retained pairing reported separately. |
| Calendar (§3.4) | First week starts <=12px below weekday label text. Cashflow tile row uses the same tile checks. |
| Tax tables (§3.5) | Independent stacks with consecutive group gaps matching CSS row gap, no ragged cross-row void; two columns where page width permits, one with dock/narrow page. Preserve jurisdiction reading order and report actual column count. |
| Allocation (§3.6) | Card-height spread <=1px across Asset class, Industry, Geography, Account, Holding type; aside scroll box/pinned header and plot remain contained. |
| Meters (§3.7) | Budget meter right edges and Paycheck pace track right edges each spread <=1px; empty budget track differs from card background. |
| Guide (§3.8) | Numbered rail shows Step N of M, Previous/Next select neighbor and focus the selected rail row; unnumbered rail has Next only. |
| Tiles (§4, updated user direction) | Every visible real tile row: shared value baseline <=1px and equal tile bottoms within each visual row; badge shares the title header at its right edge without overlap or a dedicated badge row. Mixed rows have real second lines except the documented Realized gains / household-wallet exceptions; named §4.4 tiles are explicitly required, with the current withholding branch attributed. Five tiles lay out 5, 3+2, or 2+2+1 only when the last spans the full width (never orphan 4+1 or unfilled 2+2+1). Repeat with the metric detail dock open. At 1440, Overview/Net worth/Spending deltas each fit one line. |
| Month stability (§4.3) | Traverse every month offered by Net worth and Spending, both themes; row-height spread <=1px. The full matrix records this at every target viewport, including audit-sensitive 1280. |
| Loading/CLS (§4.6, §9) | Fresh context/route for all 14 navigation destinations plus distinct tile/editor tabs. Record browser layout-shift entries without recent input and require CLS <0.1 per route/case. Observe cold five-tile skeletons and settled rows; no phantom loading placeholders. |
| Errors (§9) | No native dialog, page error, console error, failed API response, unexpected blocked write or route fallback. Known pre-existing observations stay explicit failures/notes until coordinator disposition, never an automatic green allowlist. |

The layout walker includes all in-scope tab variants: Settings five tabs, Spending overview/trends/budgets/history, Portfolio overview/holdings/income/allocation/manage, Taxes summary/inputs/tables/what-if, Paycheck summary/profiles/changes, Comp summary/vesting/manage, ESPP summary/purchase/lots, Cards rewards/manage/lines/detail, Calendar grid/list, Projection planning/trend, Monthly balances/spending/review, Guide chapters, plus Overview and Net worth. A missing data-dependent section is a stated coverage gap.

## 4. Mutation and focus evidence (§5–§6)

Existing real-browser lane evidence is imported into a manifest with source file, commit, theme/viewport, assertion count, errors and limitations. This avoids pretending old evidence was run on merged main. Re-run representative flows on the fresh writable V twin after merge; the backend exact-cascade suite remains the exhaustive data-image gate.

| Lane | Evidence and fresh integrated checks |
|---|---|
| L5 Settings | Original audit scratch `work-L5/{flows-results,forms-results,activity-results,after,after-natural}.json`; final review fix `ef557961`. Last-row account/category Edit focuses fully visible selected field; Escape/Cancel returns original row; create flashes and reveals; validation/error within200px or sticky bar; delete/Undo restores identical ids; kind/retire Undo; Activity older-page fallback and report reveal; irreversible confirmation Cancel/Escape sends no write. Review fixes have focused tests and will be sampled after merge. |
| L6 money A | `%TEMP%/codex-polish-L6/work-L6/{flows,extras}.mjs` and result JSONs. Eight CRUD families with exact API equality/focus/busy widths; Reset/model, manual quote, allocation draft/activate. Fresh sample security plus dependent rows or transaction, ESPP Reset/Undo, profile save/delete/Undo; confirm per-lane evidence schema before importing. |
| L7 money B | `.worktrees/polish-money-b/work-L7/{browser-report,supplement-report}.json`, product `40a65fe9`. Cards/category/detail exact Undo, calendar identity and generated overrides, budget validation/history Undo, matrix Enter/Escape/save, three-pin capacity and both Unpin paths, assistant irreversible removal. Original main script had locator-only final failure; supplement finished remaining cases. Do not mark the original report wholly green. Fresh sample card/category cascade Undo, event delete/Undo, budget named-field error, matrix draft guard, fixture-only finding confirmation. |
| L8 taxes/monthly | Coordinator provides final browser artifacts/commit. Fresh sample local Tax Inputs SaveStatus, discard guard Cancel/confirm, What-if Apply/Undo including dirty-input guard, bracket/table confirmation, Monthly button-specific busy state and part delete/Undo. All tax/month scopes remain the initiating scope during delayed settlement. |

For each fresh mutation case record: initiating control/idle width; pending control `aria-busy` and inert siblings; focused element; errors' control distance; row visibility and `data-flash`; DOM chronology (row disappearance precedes success toast); exact API snapshot equality after Undo, ids included; no native dialog. Every created record is cleaned and every changed existing row is Undone in `finally`; cleanup failure makes the run fail and reports exact local ids. Irreversible checks use temporary fixtures or stop at Cancel. Never remove existing saved findings, revoke existing feed links, apply imports/restores or modify production.

## 5. Gates, review and handoff

- [x] Validate probe syntax and argument/preflight refusal cases before browser use. A probe-only change does not need the whole frontend suite.
- [x] Coordinator runs integrated app/node `tsc -p --noEmit`, lint (0 errors, <=26 baseline warnings), full Vitest with bounded workers, `vite build`, backend `pytest -n4`. Never `tsc -b` in this junction worktree. Import exact command/head/count/log evidence into V report.
- [x] Run final matrix only after merged main notification. Inspect screenshots for threshold failures and representative success cases; classify instrument mistakes separately and retain rerun provenance.
- [x] Send concrete product findings to coordinator promptly; re-run only affected cases after fixes, then complete outstanding matrix coverage. Do not broaden scope to unrelated audit items.
- [x] Commit plan/probe under `feat/polish-verify`; no merge/push/deploy. Hand off product SHA, fresh-copy provenance, before/after metrics, exact coverage, gate results, deviations, artifact paths and remaining failures. Stop owned Vites/backends and close browsers; leave the transferred user preview running.

## Historical preparation record

2026-09-26: worktree created at `204ef73d`, then rebased to merged product `350b91a8b4fed2fa107da5bcc370682ecaceef60` (all four lanes plus the final ESPP test wait). Original audit/L1/L2/L5 helpers recovered; no product code changed here.

Fresh source acquired **07:33:31Z**, completed07:33:35Z: **1,017,604 bytes**, SHA-256 `bb765dd3c70f20cc16f9d741936dcc6db1d0b36f83d55b58805bc1b084e210a8`. Both copies contain **45 tables /27,244 rows**, identical per-table counts, schema `f12026092301`. The production host received only its established read-only `pg_dump` command. A fresh local JWT was signed for the existing copied user; no password or copied user row changed. Both private `/auth/me` checks returned200. Read database default transactions are read-only.

Preparation artifacts live in ignored `scratchpad/polish-v/`: `source.json`, dump, token, private runtime/config/log files, `lane-evidence.json`, `prep-sidebar/`, `prep-pairs/`, `prep-layout/`. Initial sidebar smoke exposed an obsolete probe selector (`.sidebar-footer-row`); it now measures the actual footer children's common center. Sidebar plus five chart-pair/Table cycles at1440dark passed **35 checks**. Main layout selectors at1440dark passed **56 checks**. These are instrument preparation records, not final acceptance, and later instrumentation additions require the final run.

The historical importer attributes **11 artifacts /62 observations** to L5–L8 and explicitly retains L5's injected failure and L7's original last-locator timeout as partial records. It rejects zero/unrecognized records, false exact-Undo/flash/focus flags, nested native-dialog/transport/fenced-write errors and cleanup failures claimed as green. Six deliberately bad fixture artifacts were refused as expected. Independent coverage review also added active-month proof, required observed cold skeleton geometry, explicit mixed-tile sites/What-if results, actual Table appearance/reveal/disappearance, Settings section selection and the tax-container/dock breakpoint. Syntax checks and `git diff --check` pass.

Coordinator's integrated code gates at `350b91a8`: **324 frontend files /4,867 tests passed (245.65s)**; backend **2,845 passed /4 skipped (90.14s)** with unchanged backend tree; app/node noEmit clean, lint0errors/26baseline warnings, production build14.11s. The first combined run's one ESPP test race (second Edit clicked before prior reload ended) was fixed with an actionable wait, then the whole frontend reran green. Final browser acceptance remains separate. Fresh writable runs are serialized L6 → L8 → L5 → L7, then the read-only matrix. A fresh L6 sticky-header focus finding is under coordinator investigation before the final matrix.

The shared reveal fix is now included from product **`b2b07a8edb3c1e7055cfe4e1b635eaf76f67b10b`**: focus/select run before the final scroll so a native date field cannot interrupt its reveal. V rebased cleanly and verified that the served source has this order. The coordinator observed the regression red before the fix, then 19 helper tests green. Final gates at this product head: **324 frontend suites /4,868 tests passed (310.08s)**; app/node types clean; ESLint **0 errors /26 existing warnings**; build **17.19s**. Logs are root `scratchpad/codex-takeover-final-{vitest3,types3,eslint3,build3}.log`. Backend remains **2,845 passed /4 skipped (90.14s)** with tree `9dcde78043e5d9dbb13e1ed53de82c1e601919c6`, log `scratchpad/codex-takeover-final-pytest.log`.

The first fresh L6 run on this fix completed all 16 behavioral checks and exactly restored nine API collections, but the probe's layout-shift descriptor assumed every attribution source was an Element. The observer now handles text nodes explicitly and records page-error stacks. That original run remains instrument-failed evidence; a clean replay is required. This probe correction does not change product code or its gate attribution.

## Fresh integrated flow and final gate record

The completed integrated gate before the What-if correction is **`c30c76bcc7c8808f642788e7b5f41c8db8550bd3`**. Fresh L8 chronology instrumentation found that Monthly Update announced a part deletion before retiring its control. The coordinator fixed that ordering, observed four new deferred-refresh regressions fail before the fix, and passed all 190 Monthly tests afterward. Integrated gates at this head: **324 frontend suites /4,872 tests passed (271.54s)**, app/node noEmit clean, ESLint **0 errors /26 existing warnings**, build **17.92s**. Root logs are `scratchpad/codex-takeover-final-{vitest5,types5,eslint5,build5}.log`. Backend remains the unchanged **2,845 passed /4 skipped (90.14s)** result above.

| Fresh evidence | Product and completed coverage | Restoration/error evidence |
|---|---|---|
| L5 Settings | `b2b07a8e`, 55 checks; `%TEMP%/codex-polish-L5/work-L5-integrated/manifest.json` | Five fixture collections empty afterward; original household/settings values restored. Two intentional 409/500 responses remain explicitly attributed in the fallback artifact; zero unexpected errors. |
| L6 money A | `b2b07a8e`, 16 checks; `%TEMP%/codex-polish-L6/work-L6-integrated/2026-09-26T08-20-11.037Z/results.json` | Eight temporary records removed, nine API collection hashes/counts restored exactly; period Reset and Allocation targets Undone. Zero browser/network errors, dialogs, blocked writes or retries. Normal/reduced native-date focus trace is adjacent. |
| L7 money B | `c30c76b`, 15 checks in `scratchpad/polish-v/fresh-L7-final/browser-report.json` plus 5 in `fresh-L7-settled/supplement-report.json` | Exact card/dependent/category, calendar event, budget history and override Undo. Five API collection hashes and the entire budget table restored. Final logs captured after context close are clean; card removal preceded its toast by 42.1ms. |
| L8 taxes/monthly | `c30c76b` tax/vesting flows plus `0b3290c2` affected What-if/Monthly replay, four dark/light groups; root `scratchpad/codex-takeover-v-l8/manifest.json` | Exact tax/vesting/What-if/review/balance Undo; dirty-draft Cancel/confirm and menu guards; latest Monthly control removal preceded toast by 50.5ms dark /61.3ms light. Final post-close logs are clean. Earlier c30 replay is retained separately. |

The only product change from `b2b07a8e` to `c30c76b` is Monthly Update and its regressions; L5/L6's product attribution stays explicit. Their unchanged surfaces are not misrepresented as newly rerun at `c30c76b`.

Retained nonfinal attempts include L6's text-node observer failure, L8's original Monthly chronology failure and a background-request teardown error, and L7's early sample of an earlier identical "Restored" toast. The L7 replacement waits for the initiating Undo response/new restoration toast and records focus immediately and at 16/50/250ms: every sample remains on the restored event button. Fresh drivers settled pending requests and inspected logs after closing contexts; the later complete-callback drain strengthened this lifecycle further. At this historical stage the final matrix and screenshot review had not yet run; their results lead this document.

Focused preflight also verifies the rendered month, not only its selected ribbon chip: Net worth labels/values must match the requested API summary (including early/provisional as-of dates), Spending must render that month's label, and loading dimming must finish. All 38 months per route passed at 1280 dark; Net worth's row stayed 159.2px. Calendar Grid/List and all three Monthly steps assert their actual mode/content. At 1440 the full matrix additionally asserts every month's delta line count.

The first full attempt retained 49 completed cases in `scratchpad/polish-v/final-c30/report.json`. It exposed two probe assumptions: 1280 legitimately falls back to a modal overlay (the dock requires 1290px), and ESPP's hint-only tiles need the real Assistant panel to exercise the dock. Both modes now assert their expected viewport behavior. Focused 1280/1366 checks also exposed a dynamic chart-label casing mismatch (`innerText` includes CSS uppercase); raw `textContent` fixes that measurement. The six affected Table checks then passed, each revealing the full 363.78px scroll box below the sticky scope, compared with the audit's 21px.

That same full attempt found **real** Tax What-if CLS violations: 0.122786 for the override scenario and 0.248445 for the sale scenario at 1280 dark. The coordinator reproduced both in dark/light (`scratchpad/codex-takeover-whatif-cls-before/report.json` on root main). These remain retained product findings, separate from probe issues. Their settled screenshots were visually inspected: controls and results are legible, while recorded shift entries establish the loading transition failure that a final screenshot alone cannot prove. Representative L7/table success screenshots and the failed overlay navigation screenshot were also inspected; notes are in ignored `scratchpad/polish-v/visual-review.json`.

The historical What-if correction was **`0b3290c2230827fd4ff38541e8794025a26b1b8e`**, which stages the first What-if feeds/preview and keeps its controls wrapper stable. V rebased cleanly and confirmed the served source. The coordinator reported 64 affected tests, app noEmit and targeted lint passing, with independent review clear. The twenty cold What-if cases (two scenarios in every viewport/theme combination) passed **160/160 checks**, with no driver errors and maximum CLS **0.023983**; root artifact `scratchpad/codex-takeover-whatif-cls-after/report.json`. V visually inspected the corrected 1280 dark override and 1440 light sale screenshots: coherent full-width controls, aligned result tiles, and no overlap or clipped controls. The later replay and gates are recorded below; the earlier c30 gates are not attributed to this product head.

The affected What-if/Monthly mutation replay subsequently passed dark and light at `0b3290c2`: exact What-if inputs, complete review and balances restored by Undo; dirty-draft Cancel/confirm passed; Monthly controls disappeared 50.5ms/61.3ms before their toast. The updated L8 manifest preserves per-artifact heads and hashes, and retains its earlier c30 results separately. V revalidated all ten current fresh artifact hashes and the superseded c30 artifact.

The next full frontend run at `0b3290c2` recorded **4,875 passing tests and one asynchronous TransactionsPanel assertion failure** (323 passing suites, one failing; 273.50s). The assertion looked for the reset form before React committed it; test-only fix **`01b1aed3`** waits for the resulting Add transaction control, and **73/73** affected tests passed afterward. The user then explicitly requested the footer/badge cosmetics and no further heavyweight test run. Preserve this attribution rather than describing the latest source as having a new fully green full-suite run. The last complete green suite is the c30 result above; current cosmetic focus checks and the final browser matrix are separate evidence.
