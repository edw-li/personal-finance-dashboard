.venv
**/.pytest_cache
**/.ruff_cache
**/__pycache__
tests
docker-compose.yml
.env
# Snapshots and restore points (DATA_DIR): prod mounts the finance-data volume at /data,
# but a dev box writes backend/data — COPY . . would bake real financial data into the image.
data/
